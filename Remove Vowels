def remove_vowels():

    word = input('Input: ')
    vowels = 'a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U'
    result = ''

    word = [char for char in word if char not in vowels]
    print('Output:', result.join(word))

remove_vowels()
